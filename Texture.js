'use strict'
class Texture
{
    constructor(texFile, texUnit, texType = gl.TEXTURE2D)
    {
        this.texFile = texFile;
        this.texUnit = texUnit;
        this.texType = texType;
    }

    loadTexture()
    {
        
    }

    loadOver()
    {
        
    }
}